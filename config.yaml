data:
  block_size: 128
  batch_size: 64

model:
  d_model: 256
  num_heads: 8
  num_layers: 6
  max_len: 128

training:
  epochs: 1
  lr: 0.0003
  weight_decay: 0.01
  grad_clip: 1.0
  device: cpu   # keep cpu for now (CI friendly)

logging:
  save_every: 1
